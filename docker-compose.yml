# docker-compose.yml
version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      MONGO_URI: 'mongodb://mongo:27017/users'
  mongo:
    image: mongo
    ports:
      - '27017:27017'
